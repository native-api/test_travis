os: linux
language: generic
matrix:
    include:
        -   dist: trusty
            name: trusty
        -   dist: xenial
            name: xenial
        -   dist: bionic
            name: bionic
script:
    - sudo cat /etc/docker/daemon.json